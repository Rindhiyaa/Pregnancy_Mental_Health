# Development environment — used when running: npm run dev
# Change VITE_API_URL to match where your backend is running
# Default: http://localhost:8000/api  (same machine)
# Other machine on LAN: http://192.168.x.x:8000/api
VITE_API_URL=http://127.0.0.1:8000
