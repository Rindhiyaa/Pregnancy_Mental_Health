# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr

# Environment files with secrets (ignore)
*.local

# Environment files without secrets (commit these!)
# .env.production - MUST be committed (Vite bakes at build time)
# .env.development - MUST be committed (no secrets, just URLs)
# .env - Can be committed (no secrets)

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
